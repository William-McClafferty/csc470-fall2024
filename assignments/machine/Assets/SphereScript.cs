using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SphereScript : MonoBehaviour
{
    // Start is called before the first frame update (Only at the beginning)
    void Start()
    {
        
    }

    // Update is called once per frame (The whole time)
    void Update()
    {
        
    }
}
